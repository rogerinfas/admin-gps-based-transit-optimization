/**
 * Archivo placeholder. Se sobrescribe ejecutando `npm run generate`,
 * que genera tipos a partir del OpenAPI del backend NestJS.
 *
 * Comando esperado:
 *   npm run generate
 *
 * Esto produce los tipos `paths` y `components` que utilizan
 * `openapi-fetch` y `openapi-react-query` (ver docs/API_CLIENT.md).
 */
export type paths = Record<string, never>;
export type components = { schemas: Record<string, never> };
